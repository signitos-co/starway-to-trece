function Label(x, y, content, color, size, font, center) {
    this.x = x
    this.y = y
    this.content = content
    this.color = color
    this.size = size
    this.font = font
    this.center = center
}